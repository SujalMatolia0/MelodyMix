import { IconBrandGoogleFilled } from '@tabler/icons-react';
import { type ReactNode } from 'react';

export const OAuthIcons: Record<string, ReactNode> = {
  Google: <IconBrandGoogleFilled size={18} />,
};
